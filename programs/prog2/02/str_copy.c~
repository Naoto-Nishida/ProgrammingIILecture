#include <stdio.h>
 
int main(void)
{
    char str1[] = { 'm', 'a', 's', 't', '\0' }; /* "mast" と同じ */
    char str2[5];
 
    str2 = str1;    /* 配列 str1 を配列 str2 にコピーしているつもり */
 
    printf("str1 = %s\n", str1);
    printf("str2 = %s\n", str2);
 
    return 0;
}
